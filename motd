                    Ether — Zomato Admin Shell

  This container is the operational admin shell for Ether-based hosts.
  The host filesystem is available under /.ether/rootfs.

  Use the `ether` command (`sudo ether`) to drop into a root shell on
  the host, or `ether <cmd>` to run a single command on the host.
